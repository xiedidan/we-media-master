# 020-中心思想输入功能

## 背景
当前用户只需要输入主题和关键词，Agent自行理解写作方向。缺少明确的中心思想引导，导致文章容易"散"。

## 目标
增加中心思想输入字段，让用户明确文章的核心观点，Agent围绕中心思想写作。

## 验收标准
- [ ] 在UI中添加中心思想输入框
- [ ] 将中心思想传递到prompt构建中
- [ ] 在生成文章时体现中心思想

## 实施记录
- 在app.py输入区域添加"中心思想"文本输入
- 在build_user_prompt中添加central_idea参数
- 更新prompts.yml添加central_idea说明